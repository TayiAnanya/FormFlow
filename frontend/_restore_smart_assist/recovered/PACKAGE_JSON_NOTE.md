# pdfjs-dist dependency (Smart Assist)

Add to `frontend/package.json` dependencies:

```json
"pdfjs-dist": "4.10.38"
```

Then run:

```bash
cd frontend
npm install pdfjs-dist@4.10.38
```

Do **not** blindly overwrite the whole `package.json` — only add this dependency.
