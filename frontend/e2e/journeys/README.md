# Journeys (reserved)

Intentionally empty in v1. Cross-feature journeys live inside feature modules (`workspace`, `forms`, `advisor`, `quality`).

Do not add business page objects here until a future sprint explicitly owns this folder.
