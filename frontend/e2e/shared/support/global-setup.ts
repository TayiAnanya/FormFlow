/**
 * Optional global setup — auth storageState generation reserved for Sprint 01+.
 * Sprint 00 keeps this as a no-op hook for playwright.config.
 */
async function globalSetup(): Promise<void> {
  // Intentionally empty for Sprint 00.
}

export default globalSetup;
