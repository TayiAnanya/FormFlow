# Shared workflows

Sprint 02 — navigation composers (assertion-free).

| Module | Role |
|---|---|
| `navigation.workflow.ts` | Shell menu, brand, deep links, logout nav, history |

Import from `e2e/shared/workflows`. Reuses `PortalShellPage` and Sprint 01 `loginWithReturnUrl`.
